// greetings.js

/**
 * Generates a greeting message for the given name.
 * @param {string} name - The name to greet.
 * @returns {string} The greeting message.
 */
function greet(name) {
    return `Hello, ${name}! Welcome to Java and JavaScript integration.`;
}

// Optional: Add an export statement if needed for further modularization.
greet;
